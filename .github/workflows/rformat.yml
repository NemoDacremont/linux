name: Rust format

# Became obsolete in favor of rs-tests.yml, only enable manual run
on:
  workflow_dispatch:
  # pull_request:
  #   paths:
  #     - 'drivers/**/*.rs'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Test rust format
      run: make -C pfa rust_testformat
