name: C/C++ CI

on: 
  pull_request:
    paths:
      - 'drivers/**/*.[ch]'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Test code formatting
      run: make -C pfa test_format
